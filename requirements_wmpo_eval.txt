# =============================================================================
# WMPO/SWM 평가 환경 (wmpo conda env) — 검증된 버전 고정
# 작성: 2026-06-02 / GPU: RTX PRO 6000 Blackwell (sm_120)
# =============================================================================
# 주의:
#  - torch/torchvision/torchaudio는 cu128 인덱스에서 설치 (Blackwell 지원).
#    pip install torch==2.8.0 torchvision==0.23.0 torchaudio==2.8.0 \
#      --index-url https://download.pytorch.org/whl/cu128
#  - flash-attn은 prebuilt wheel 직접 설치 (소스 빌드 X, nvcc 불필요):
#    https://github.com/Dao-AILab/flash-attention/releases/download/v2.8.3/
#      flash_attn-2.8.3+cu12torch2.8cxx11abiTRUE-cp311-cp311-linux_x86_64.whl
#  - transformers는 moojink fork (bidirectional attn, OpenVLA-OFT parallel decoding):
#    pip install git+https://github.com/moojink/transformers-openvla-oft.git
#  - mujoco-py는 native MuJoCo 2.1.0 (~/.mujoco/mujoco210) + Cython<3.0 필요.
#  - robosuite/robomimic/mimicgen/robosuite-task-zoo/LIBERO/opensora는
#    WMPO 레포 dependencies에서 editable(--no-deps) 설치 (setup.sh 참고).
#  - 설치 순서가 중요함 → setup_wmpo_full.sh 사용 권장.
#  - 전 과정 PYTHONNOUSERSITE=1 권장 (user-site 패키지 shadowing 방지).
# =============================================================================

# --- core DL ---
# torch==2.8.0+cu128         (cu128 인덱스, 위 주석 참고)
# torchvision==0.23.0+cu128
# torchaudio==2.8.0+cu128
numpy==1.26.4

# --- VLA / OpenVLA-OFT ---
# transformers==4.40.1       (moojink fork, 위 주석 참고)
tokenizers==0.19.1
huggingface-hub==0.31.1
timm==0.9.16
peft==0.11.0
diffusers==0.33.0
xformers==0.0.28.post3
# flash-attn==2.8.3          (prebuilt wheel, 위 주석 참고)
json-numpy==2.1.1

# --- simulation (MuJoCo / robosuite stack) ---
mujoco==3.7.0
mujoco-py==2.1.2.14
cython==0.29.37
# robosuite / robomimic / mimicgen / robosuite-task-zoo / LIBERO : editable (setup.sh)

# --- TF (openvla-oft 전처리/데이터) ---
tensorflow==2.19.0
tensorflow-datasets==4.9.10
tensorflow-graphics==2021.12.3

# --- verl (RL trainer / eval 파이프라인) ---
ray==2.55.1
codetiming==1.4.0
tensordict==0.12.4
torchdata==0.11.0
pandas==3.0.3
webdataset==1.0.2
jsonlines==4.0.0
pylatexenc==2.10

# --- opensora world-model 의존성 (import-only, wm.enable=False여도 필요) ---
colossalai==0.4.7
galore-torch==1.0
bitsandbytes==0.49.2
mmengine==0.10.7
av==17.0.1
imageio==2.37.3
# tensornvme : 빌드 불가 → stub 처리 (setup.sh 참고)

# --- 기타 ---
opencv-python==4.11.0.86
h5py==3.16.0
einops==0.8.2
omegaconf==2.3.0
hydra-core==1.3.2
